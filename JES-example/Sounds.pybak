# Play sound
def playSound(): 
  file = pickAFile() 
  if file != None and file.endswith(".wav"):
    sound = makeSound(file)
    play(sound) # similar to the makePicture(..) for images
  else:
    print("Invalid file selected. Please choose a valid WAV file.")
  
def exploreSound():
  file = pickAFile() 
  if file != None and file.endswith(".wav"):
    sound = makeSound(file) 
    explore(sound)
  else:
    print("Invalid file selected. Please choose a valid WAV file.")

